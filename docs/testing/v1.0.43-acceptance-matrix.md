# v1.0.43 Acceptance Matrix

Task: `V1043-M5-05`

## Fixed CI Entrypoint

Run the non-signing release gate from the repository root:

```powershell
powershell -ExecutionPolicy Bypass -File scripts\ci\v1043-acceptance.ps1
```

List the fixed steps without running them:

```powershell
powershell -ExecutionPolicy Bypass -File scripts\ci\v1043-acceptance.ps1 -List
```

Run the signed Windows artifact lane when signing credentials are available:

```powershell
powershell -ExecutionPolicy Bypass -File scripts\ci\v1043-acceptance.ps1 -ReleaseArtifacts
```

The script uses `python` from `PATH` unless `PYTHON` is set.

## CI Lanes

| Lane | Command source | Blocks release | Coverage |
| --- | --- | --- | --- |
| `quality:diff-check` | `git diff --check` | Yes | Whitespace and patch hygiene |
| `backend:unit` | backend unittest files | Yes | auth, repository, commit, device, object storage, backup/recovery helpers |
| `backend:json-smoke` | `tests/e2e/sync_server_smoke.py` | Yes | development JSON-store sync path |
| `backend:sqlite-smoke` | `tests/e2e/sync_server_smoke.py` with SQLite profile | Yes | SQLite repository profile compatibility |
| `backend:production-smoke` | `tests/e2e/sync_server_production_smoke.py` | Yes | production auth, object storage, CAS, device revoke, capability expiry |
| `vault-core:unit` | `packages/vault-core/tests/*` | Yes | manifest, filemap, sync client, recovery, journal, tombstone, file versions |
| `desktop-core:unit` | `clients/desktop/tests/*` and `packages/ai-core/tests/*` | Yes | desktop service/CLI, E2EE, recovery package, AI compiler |
| `desktop:e2e-sync` | `tests/e2e/desktop_sync_smoke.py` | Yes | two-device desktop sync smoke |
| `frontend:lint` | `npm.cmd --prefix apps/frontend/noteapp-web run lint` | Yes | TypeScript contract check |
| `frontend:build` | `npm.cmd --prefix apps/frontend/noteapp-web run build` | Yes | Vite production build |
| `frontend:entry-policy` | `npm.cmd --prefix apps/frontend/noteapp-web run smoke:entry-policy` | Yes | V1 navigation and desktop diagnostics entry policy |
| `frontend:e2e-dist` | `tests/e2e/frontend_dist_smoke.py` | Yes | built SPA serving and fallback |
| `frontend:e2e-sync-snapshot` | `tests/e2e/frontend_sync_snapshot_smoke.py` | Yes | bridge snapshots, workspace files, settings, sync action wiring |
| `desktop:build-frontend` | desktop package frontend build | Yes | desktop shell bundle input |
| `desktop:dist-win-signed` | `npm.cmd --prefix apps/desktop/noteapp-desktop run dist:win:signed` | Release artifact lane | signed installer and Authenticode verification |

## P0 Task Coverage

| P0 scope | Automated or smoke gate | Status |
| --- | --- | --- |
| `V1043-M0-01` to `V1043-M0-04` entry strategy and workspace entry | `frontend:lint`, `frontend:build`, `frontend:entry-policy`, `frontend:e2e-sync-snapshot` | Covered |
| `V1043-M1-01` to `V1043-M1-06` move note and sync/conflict entry | `vault-core:unit`, `desktop-core:unit`, `desktop:e2e-sync`, `frontend:e2e-sync-snapshot` | Covered |
| `V1043-M2-01` to `V1043-M2-07` E2EE, recovery, resumable blob transfer | `vault-core:unit`, `desktop-core:unit`, `backend:json-smoke`, `backend:sqlite-smoke` | Covered |
| `V1043-M2-13` to `V1043-M2-16` devices, tombstone GC, sync safety regression | `backend:unit`, `backend:production-smoke`, `vault-core:unit`, `desktop:e2e-sync` | Covered |
| `V1043-M3-01` to `V1043-M3-07` production backend | `backend:unit`, `backend:sqlite-smoke`, `backend:production-smoke` | Covered |
| `V1043-M5-01` desktop signing | `desktop:dist-win-signed` in release artifact lane | Covered when signing credentials exist |
| `V1043-M5-02` desktop diagnostics | `frontend:entry-policy`, `desktop:build-frontend` | Covered |
| `V1043-M5-03` manual update strategy | documentation review in `docs/develop/v1.0.43-desktop-update-strategy.md` | Covered by release checklist |
| `V1043-M5-04` user docs | documentation review in `docs/user/` | Covered by release checklist |
| `V1043-M5-05` acceptance matrix | this document and `scripts/ci/v1043-acceptance.ps1` | Covered |
| `V1043-M5-06` long stability regression | dedicated stability script/report | Pending next task |
| `V1043-M5-07` RC acceptance | RC report consuming this matrix and M5-06 report | Pending |

## Section 17 Matrix

| Section 17 item group | Gate | Notes |
| --- | --- | --- |
| 17.1 items 1-2: create/edit/delete/rename/move notes and attachments | `desktop-core:unit`, `frontend:e2e-sync-snapshot`, `desktop:e2e-sync` | Covers service/CLI operations and bridge visibility |
| 17.1 items 3-6: backlinks, search index, autosave, draft recovery | `vault-core:unit`, `frontend:lint`, `frontend:build`, `frontend:entry-policy` | Detailed UI walkthrough remains part of RC manual pass |
| 17.1 items 7-8: recovery package + phrase, reject missing recovery package | `desktop-core:unit` | Covered by crypto/recovery CLI and service tests |
| 17.2 items 1-5: basic sync, concurrent edits, conflict, offline recovery | `desktop:e2e-sync`, `vault-core:unit`, `backend:json-smoke`, `backend:sqlite-smoke` | Two-device smoke plus protocol unit coverage |
| 17.2 item 6: wrong password | `desktop-core:unit` | E2EE provider and recovery import rejection |
| 17.2 item 7: interrupted upload resume | `vault-core:unit`, `backend:unit` | Chunk descriptors and upload capability/state coverage |
| 17.2 items 8-10: corrupt manifest, object storage fault, old device catch-up | `backend:production-smoke`, `vault-core:unit` | Production smoke covers object storage and invalid manifest paths |
| 17.2 items 11-19: stable file ID, path collisions, server manifest validation, deleted revision | `vault-core:unit`, `backend:unit`, `desktop-core:unit` | Blocks app rename/move regression and illegal manifest acceptance |
| 17.2 items 20-28: ack, device list, tombstone eligibility, dirty protection | `backend:production-smoke`, `vault-core:unit`, `desktop:e2e-sync` | Device revoke and ack state are covered in production smoke |
| 17.2 items 29-48: conflict orphan, crash recovery, journal, staging, stale summary gates | `vault-core:unit`, `desktop-core:unit` | Unit suite is the release blocker for these edge cases |
| 17.2 items 49-61: empty vault first commit, migration baseline, queued operations, device revoke, credential expiry, legacy tombstone upgrade | `vault-core:unit`, `backend:production-smoke`, `desktop-core:unit` | Long offline resurrection is additionally covered by `V1043-M5-06` |
| 17.3 items 1-8: AI summary, wiki Q&A, source jump, refusal, provider fault | `desktop-core:unit`, `frontend:lint`, `frontend:build` | External provider behavior requires RC manual/provider smoke |
| 17.3 items 9-15: AI writeback confirmation, raw cache boundary, superseded tasks | `desktop-core:unit`, `frontend:entry-policy`, docs review | Cross-device AI privacy/writeback pass remains in RC checklist |
| 17.4 performance hard gates | `V1043-M5-06` stability report and RC benchmark notes | No release without recorded benchmark result |
| 17.5 launch gates | Full `v1043-acceptance.ps1`, signed artifact lane, `V1043-M5-06` | Feeds `V1043-M5-07` RC report |

## Release Checklist Inputs

Before `V1043-M5-07`, collect:

1. Output of `scripts\ci\v1043-acceptance.ps1`.
2. Output of `scripts\ci\v1043-acceptance.ps1 -ReleaseArtifacts` or an explicit signing-lane waiver.
3. `V1043-M5-06` 1000-sync and long-offline report.
4. Manual RC notes for AI provider behavior, performance gates, and user-documentation review.

## Known Deferred Automation

The matrix intentionally does not hide residual manual work:

1. 1000 consecutive sync and long-offline tombstone resurrection are owned by `V1043-M5-06`.
2. Performance hard gates need recorded RC benchmark output before `V1043-M5-07`.
3. External AI provider behavior cannot be fully deterministic in unit tests; RC must include a configured-provider smoke or a documented provider outage/degraded-mode pass.
