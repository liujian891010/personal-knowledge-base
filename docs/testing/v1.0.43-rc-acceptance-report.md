# v1.0.43 RC Acceptance Report

Task: `V1043-M5-07`

Date: 2026-05-18

Decision: PASS for the non-signing RC gate. The signed Windows artifact lane is waived in this sandbox because signing credentials are not configured; it remains required before public distribution.

## Evidence Set

| Evidence | Result | Notes |
| --- | --- | --- |
| `scripts\ci\v1043-acceptance.ps1` | PASS | Completed on 2026-05-18 from repository root |
| `tests\e2e\desktop_sync_stability.py --iterations 1000` | PASS | Recorded in `docs/testing/v1.0.43-stability-regression.md` |
| `node apps\desktop\noteapp-desktop\scripts\check-win-signing-env.mjs` | WAIVED | Current sandbox has no `CSC_LINK` and `CSC_KEY_PASSWORD` |
| User documentation review | PASS | `docs/user/README.md`, user manual, privacy notes, FAQ |
| Update strategy review | PASS | V1 remains manual update only |

## Default Acceptance Gate

Command:

```powershell
powershell -ExecutionPolicy Bypass -File scripts\ci\v1043-acceptance.ps1
```

Result: PASS.

Covered lanes:

| Lane | Result |
| --- | --- |
| `quality:diff-check` | PASS |
| `backend:unit` | PASS, 37 tests |
| `backend:json-smoke` | PASS |
| `backend:sqlite-smoke` | PASS |
| `backend:production-smoke` | PASS |
| `vault-core:unit` | PASS, 181 tests |
| `desktop-core:unit` | PASS, 320 tests |
| `desktop:e2e-sync` | PASS |
| `frontend:lint` | PASS |
| `frontend:build` | PASS, bundle `index-B5mwzphZ.js` |
| `frontend:entry-policy` | PASS |
| `frontend:e2e-dist` | PASS |
| `frontend:e2e-sync-snapshot` | PASS |
| `desktop:build-frontend` | PASS |

The acceptance wrapper now fails immediately when a native child command returns a non-zero exit code. The two frontend smoke tests also isolate user-profile, app-data, temp, and npm cache paths so sandbox-local runs cannot read fixture or profile state from the host user directory.

## Stability Gate

The `V1043-M5-06` release-blocking run passed with SQLite storage:

| Scenario | Result | Evidence |
| --- | --- | --- |
| 1000 continuous syncs | PASS | `head_revision=1000`, final hash `a6af2cecaec264d65bc772716f8b6993e9f43f50e22e921bc159a5ba07ef5411` |
| Long-offline delete | PASS | `canonical_path_exists_after_reconnect=false`, `stale_submit_created_commit=false` |

## Release Coverage

| Gate | Status | Evidence |
| --- | --- | --- |
| Installable desktop shell input | PASS | `desktop:build-frontend` |
| Edit, rename, move, delete notes | PASS | `desktop-core:unit`, `frontend:e2e-sync-snapshot` |
| Attachments | PASS | `desktop-core:unit`, bridge workspace mutation coverage |
| Encrypted sync and recovery flow | PASS | `desktop-core:unit`, `vault-core:unit`, backend smokes |
| Conflict and stale-device sync safety | PASS | `desktop:e2e-sync`, `backend:production-smoke`, stability report |
| AI degraded-mode behavior | PASS | `desktop-core:unit`, frontend build and entry policy; configured-provider live smoke remains environment-dependent |
| User documentation | PASS | `docs/user/` |
| Signed Windows installer | WAIVED | Signing credentials absent in current sandbox; run `-ReleaseArtifacts` with credentials before release |

## Signing Lane Waiver

The signed Windows artifact lane was not run in the current sandbox. The local signing preflight reports:

```text
Windows signing is not configured.
Set CSC_LINK and CSC_KEY_PASSWORD before running the signed Windows release build.
WIN_CSC_LINK and WIN_CSC_KEY_PASSWORD are also accepted and forwarded to electron-builder.
```

Release operators must run this command in the signing environment before publishing:

```powershell
powershell -ExecutionPolicy Bypass -File scripts\ci\v1043-acceptance.ps1 -ReleaseArtifacts
```

## RC Notes

1. Default automated acceptance is green as of 2026-05-18.
2. The long stability gate is green and linked from this report.
3. The only non-green item is the signed artifact lane, waived because credentials are intentionally unavailable in this sandbox.
4. No new acceptance criteria were invented in `V1043-M5-07`; this report consumes the fixed matrix, stability report, documentation set, and release scripts.
