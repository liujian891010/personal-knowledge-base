# v1.0.43 方案差距扫描

扫描日期：2026-05-11

对照方案：

- `docs/develop/类Obsidian软件最终可落地方案（本地优先+多端同步+AI知识库版）_v1.0.43.md`

本扫描用于判断当前仓库距离方案 V1 还缺哪些部分，并区分“已经收口”“仍需完成”“可延后”。本文件反映当前实现状态，不再沿用最初 gap-scan 中已经过期的结论。

## 当前已收口

### 1. Markdown 编辑基础闭环

已具备：

1. 选择本机工作区文件夹。
2. 初始化并导入工作区文件。
3. 展示本地文件树。
4. Markdown 编辑、预览、分栏。
5. 写回本地文件系统。
6. 保存后刷新 workspace / sync 快照。
7. 非 Markdown 文件以附件形式显示，不进入 Markdown 编辑器。
8. `.ai/` 作为系统目录从普通笔记库浏览中隐藏。
9. 可从当前 Markdown 笔记导入附件，并插入相对路径链接。

### 2. 自动保存与草稿恢复

已具备：

1. 编辑器输入后写入 `.noteapp/drafts/<file_id>.draft`。
2. 草稿写入使用服务端原子写入路径。
3. 正式保存成功后清理草稿。
4. 加载文件时检测草稿。
5. 前端提供恢复 / 丢弃草稿入口。
6. 自动保存会写回本地文件。

后续可优化：

1. 草稿冲突提示文案。
2. 大文件和二进制附件的草稿保护提示。
3. 更精细的保存状态展示。

### 3. 本地搜索 MVP

已具备：

1. SQLite search index / FTS 路径。
2. `rebuild-search-index` CLI。
3. `search-workspace` CLI。
4. bridge `/api/workspace/search`。
5. 前端搜索输入和结果跳转。
6. 搜索跳转后选中状态正确刷新。

后续可优化：

1. 搜索索引已从查询时全量 rebuild 改为变更点维护，手动 rebuild 保留为修复入口。
2. 搜索结果 snippet / 高亮仍比较基础。
3. 暂无范围过滤、类型过滤、最近文件排序。
4. 暂无搜索结果总结。

### 4. 双链 / 反链 MVP

已具备：

1. Markdown 正文 `[[...]]` 解析。
2. note links 索引写入。
3. outgoing links 与 backlinks 查询。
4. bridge links API。
5. 前端详情面板展示出链和反链。
6. 点击可跳转到目标文件。

后续可优化：

1. 重命名文件时会改写指向旧标题的 wiki link，并保留锚点和别名。
2. 未解析链接已提供创建入口，可从详情面板直接创建目标笔记。
3. 别名、标题匹配、大小写策略仍需产品化。
4. 图谱视图还不是基于真实关系的完整体验。

### 5. Markdown 文档 CRUD 与回收站

已具备：

1. 新建 Markdown 文档。
2. 重命名仅修改原文件名，不移动目录。
3. 删除进入 `.noteapp/trash`，不直接永久删除。
4. filemap 保留同一 `file_id`。
5. 回收站列表、恢复、单个彻底删除、清空。
6. 删除弹窗和重命名弹窗使用应用内 UI，不依赖浏览器原生 confirm / prompt。
7. 缺失 trash 文件可 purge，避免卡死。

后续可优化：

1. 还未实现应用内移动文件。
2. 附件 CRUD 还未完整开放。
3. 删除 / 恢复后的同步状态说明还可以更清晰。

### 6. AI Wiki 编译 MVP

已具备：

1. `packages/ai-core` 确定性编译器。
2. `compile-ai-wiki` CLI。
3. bridge `/api/ai/wiki/compile`。
4. 前端 AI Wiki 页面。
5. 生成 `.ai/index.md`。
6. 生成 `.ai/wiki/*.md`。
7. 记录 `.ai/log.md`。
8. 支持 `locked: true` / `user_edited: true` 页面跳过覆盖。
9. 支持未变化页面跳过写入。
10. AI Wiki 页面展示结构化 frontmatter。

当前限制：

1. 这是确定性编译 MVP，不是真正 LLM 总结。
2. AI Wiki Ask 已可在存在 citations 时调用已配置 provider；无 citations 时不会调用模型，并提示先编译/刷新检索。
3. 暂无搜索结果总结。
4. 暂无结果插入当前笔记。

### 7. 同步核心 MVP

已具备：

1. filemap 管理。
2. tombstone ledger。
3. SQLite vault state / journal。
4. pull / apply / commit 流程。
5. local change detection。
6. conflict copy。
7. import / export vault package。
8. sync shell snapshot 和 action dispatch。
9. 本地开发 sync server。

当前限制：

1. E2EE 仍是 placeholder。
2. 后端仍是本地开发服务，不是生产服务。
3. 设备管理 UI 和 tombstone 回收产品闭环未完成。

## V1 仍需完成

### P0：提交当前实现并保持文档同步

状态：进行中。

已提交：

1. `d85ccd1 feat: refine ai wiki and workspace attachments`

还需：

1. 本文档更新后提交。

### P1：AI 问答 MVP

状态：真实 provider 问答最小闭环已完成，仍需扩展知识沉淀和更多上下文入口。

已完成：

1. `packages/ai-core` 增加本地 `answer_ai_wiki`。
2. desktop service 增加 `answer_ai_wiki`。
3. CLI 增加 `ask-ai-wiki`。
4. bridge 增加 `/api/ai/ask`。
5. 前端 AI Wiki 页面增加 Ask 面板。
6. 回答返回引用来源、excerpt、score。
7. 无真实模型时明确标记 `local_deterministic`。
8. 接入本地设置中的真实模型 provider 配置，优先于环境变量。
9. 有 citations 时将引用上下文交给 LLM 生成自然语言回答。
10. Settings > AI 增加 provider health check/test 按钮。
11. Ask 失败时提供 bridge route / bridge connection / AI Key 的友好错误提示。
12. 无 citations 时提示先编译 AI Wiki 或刷新检索数据，并标记 `:no_citations`。

还需：

1. 支持当前笔记总结、文件夹总结、搜索结果总结。
2. 支持将回答插入当前笔记。
3. 支持将回答沉淀为新的知识页。
4. provider 失败时的诊断详情仍可继续产品化，例如展示状态码、请求 ID、模型响应摘要。

方案留档：

1. 统一设计见 `docs/develop/ai-context-task-panel-plan.md`。
2. 核心不是固定“总结/报告”，而是把当前笔记、文件夹、搜索结果或多选文档加入 AI 上下文，由用户输入任意指令。
3. `folder + preview` 和 `selected_files + preview` 已实现，已打通“选取文档进入 AI 文档页 -> 持续输入指令 -> 输出带来源结果”。
4. 下一步补“插入当前笔记”“保存为新知识页”“当前笔记/搜索结果入口”。

### P2：附件插入与预览

状态：最小闭环已完成。

已完成：

1. 前端可选择本地文件导入附件。
2. bridge 可接收附件 base64 内容。
3. desktop service 写入 `Attachments/`。
4. 附件进入 filemap，类型为 `attachment`。
5. 当前 Markdown 笔记自动插入相对路径链接。
6. 图片附件选中后可在应用内预览。

还需：

1. 拖拽导入附件。
2. PDF 基础预览或外部打开入口。
3. 附件重命名、删除、恢复策略。
4. 附件同步边界验证。
5. 大附件上传进度和大小限制提示。

### P3：搜索与双链增强

搜索增强：

1. 增量索引维护已完成。
2. 搜索高亮。
3. 搜索范围过滤。
4. 搜索结果总结。

双链增强：

1. 重命名时链接改写已完成基础策略。
2. 未解析链接创建入口已完成。
3. 别名 / 标题匹配策略。
4. 图谱视图接入真实 links 数据。

### P4：真实 E2EE

当前 `clients/desktop/crypto.py` 是 placeholder。

必须补齐：

1. vault key / content key 派生。
2. blob 加密和解密。
3. nonce 生成。
4. 本地密钥安全存储。
5. 错误密码和恢复流程。
6. 与 blob 去重、manifest、上传下载能力联调。

### P5：生产化后端

当前 `apps/backend/noteapp-server` 是本地开发 MVP。

必须补齐：

1. 账号模型。
2. 持久数据库。
3. 真实对象存储。
4. token 过期和刷新机制。
5. capability 过期与撤销。
6. tombstone garbage collection policy。
7. 跨进程 CAS 锁。
8. 部署、迁移、监控、备份策略。

### P6：设备管理与 tombstone 回收闭环

还需：

1. 设备列表 UI。
2. heartbeat / last_seen_at。
3. 每设备 acked_revision 展示。
4. 移除设备确认。
5. 移除后 token / capability 撤销。
6. 基于全部设备 ack 判断 tombstone 回收资格。

### P7：桌面端发布壳

当前主要是 Web + local bridge。

还需：

1. 正式桌面打包。
2. bridge 生命周期管理。
3. 启动时自动检查 bridge 健康。
4. 本地权限和目录访问提示。
5. 崩溃日志和诊断入口。
6. 自动更新策略。

### P8：UI 文案和乱码修复

当前部分前端中文文案存在编码异常风险。

还需：

1. 统一修复前端中文文案。
2. 补充空状态、错误态、加载态文案。
3. 对关键操作提供更明确的风险提示。

## 可延后

以下不阻塞当前桌面 V1 MVP：

1. 移动端完整实现。
2. 插件市场。
3. 多人实时协作。
4. Git / WebDAV 多协议同步。
5. 富文本所见即所得编辑器。
6. 复杂版本回滚 UI。
7. 大型 Agent 工作流平台。
8. 平台级 AI 计费系统。

## 当前建议执行顺序

1. 提交当前双链增强：未解析链接创建入口和重命名链接改写。
2. 接入真实 AI model provider。
3. 补附件 PDF / 外部打开和附件 CRUD。
4. 实现真实 E2EE。
5. 生产化后端。
6. 设备管理与 tombstone 回收闭环。
7. 桌面端发布壳。
8. UI 文案和乱码修复穿插进行。

短期下一步：进入真实 AI model provider 接入，让当前 citations 骨架生成自然语言回答。

## 2026-05-11 update: AI model provider

Implemented:
- Desktop AI Ask supports an OpenAI-compatible chat completions provider.
- Configure with `NOTEAPP_AI_API_KEY` and `NOTEAPP_AI_MODEL`; `NOTEAPP_AI_BASE_URL` defaults to `https://api.openai.com/v1`.
- `NOTEAPP_AI_TIMEOUT_SECONDS` is optional and defaults to `30`.
- The provider uses existing `.ai/wiki` citations as bounded context and returns a natural-language answer.
- Missing config or provider failure keeps the deterministic local fallback.
- Frontend Ask copy now reflects provider vs fallback behavior.

Remaining:
- No streaming responses yet.
- Provider failure diagnostics can still be refined beyond the current user-facing error hints.

## 2026-05-11 update: company AI model settings

Implemented:
- Settings > AI now exposes company model choices from the dev-guide document.
- Users only need to choose a model and enter their own AI Key from "My AI Key".
- The AI Key is stored only in local `.noteapp/settings.json`; snapshots expose only `api_key_configured`.
- AI Wiki Ask now prefers local settings provider config before environment variables.
- Provider dispatch supports `openai-completions`, `anthropic-messages`, and `google-generative-ai`.
- Settings > AI can test the configured provider through bridge `/api/ai/provider/health`.
- Ask UI now explains provider fallback, bridge route errors, and no-citation next steps.

Remaining:
- No streaming answer UI yet.
