# v1.0.43 后续待办与优化清单

更新日期：2026-05-19

本文承接 `docs/develop/v1.0.43-completion-task-table.md` 和 `docs/testing/v1.0.43-rc-acceptance-report.md`。当前 v1.0.43 编号任务已完成；以下清单记录发布前仍需闭环的事项，以及后续生产化和工程卫生优化项。

维护规则：

- 每完成一项，把复选框改为 `[x]`，状态改为 `已完成`。
- 在“完成记录”里写入完成日期、关键验证命令或提交号。
- 签名证书、密码、API key、对象存储密钥等凭据只允许放在本机安全存储或 CI secret，不得写入仓库文档、源码或 fixture。

## 发布前必须完成

| ID | 状态 | 优先级 | 待办 | 验收标准 | 完成记录 |
| --- | --- | --- | --- | --- | --- |
| [ ] POST-RC-01 | 未开始 | P0 | 在具备 Windows 代码签名凭据的环境执行签名产物验收 | 设置 `CSC_LINK`/`CSC_KEY_PASSWORD` 或 `WIN_CSC_LINK`/`WIN_CSC_KEY_PASSWORD`；执行 `powershell -ExecutionPolicy Bypass -File scripts\ci\v1043-acceptance.ps1 -ReleaseArtifacts` 通过；安装包 Authenticode 状态为 `Valid`；RC 报告补充签名 lane 结果 |  |

## 生产化后续待办

| ID | 状态 | 优先级 | 待办 | 验收标准 | 完成记录 |
| --- | --- | --- | --- | --- | --- |
| [ ] PROD-01 | 未开始 | P1 | 补齐生产账号体系 | 明确密码流、外部 IdP、MFA、账号恢复策略；服务端权限边界和 session 生命周期有测试覆盖 |  |
| [ ] PROD-02 | 未开始 | P1 | 托管数据库迁移与备份方案 | SQLite dev/test profile 与生产托管 DB 路径分离；迁移、备份、恢复流程可重复执行并有回归验证 |  |
| [ ] PROD-03 | 未开始 | P1 | 对象存储生产运维能力 | S3/OSS 凭据轮换、bucket 生命周期、失败告警和最小权限策略落地 |  |
| [ ] PROD-04 | 未开始 | P1 | tombstone GC 后台化 | 补齐后台 worker、生产 retention 配置、审计记录和误删恢复边界 |  |
| [ ] PROD-05 | 未开始 | P1 | 分布式 CAS 锁与运维告警 | 多节点部署下 commit CAS 保持单调 revision；冲突、锁等待、错误率进入监控告警 |  |

## 工程卫生与体验优化

| ID | 状态 | 优先级 | 待办 | 验收标准 | 完成记录 |
| --- | --- | --- | --- | --- | --- |
| [x] HYG-01 | 已完成 | P0 | 清理已跟踪的桌面 release 构建产物 | `apps/desktop/noteapp-desktop/release` 不再有已跟踪文件；`.gitignore` 继续覆盖 release/win-unpacked 输出；仓库体积不再随构建产物增长 | 2026-05-19；执行 `git rm -r --cached apps/desktop/noteapp-desktop/release`；验证 `git ls-files apps/desktop/noteapp-desktop/release` 为空；提交见本次 HYG-01 提交 |
| [x] HYG-02 | 已完成 | P0 | 修复桌面 lockfile 私有 registry 地址 | `apps/desktop/noteapp-desktop/package-lock.json` 不再引用 `http://192.168.3.8:4873`；新环境可按正式 registry 安装依赖 | 2026-05-19；执行 `npm.cmd install --package-lock-only --ignore-scripts --registry=https://registry.npmjs.org/`；更新 70 个私有 `resolved` 字段；验证 `rg -n "192\.168\.3\.8|http://192\.168\.3\.8" apps\desktop\noteapp-desktop\package-lock.json` 无匹配 |
| [x] HYG-03 | 已完成 | P1 | 收口前端 sync bridge URL 配置 | `http://127.0.0.1:3187` 只在共享配置模块中定义一次；所有 hook/view 复用同一入口 | 2026-05-19；新增 `apps/frontend/noteapp-web/src/syncBridgeConfig.ts`；所有 hook/view 复用 `syncBridgeUrl`；验证 `rg -n "127\.0\.0\.1:3187|VITE_NOTEAPP_SYNC_BRIDGE_URL" apps\frontend\noteapp-web\src` 仅配置模块匹配；`npm.cmd run lint` 通过 |
| [x] HYG-04 | 已完成 | P1 | 收口硬编码内部服务地址 | 登录地址和 AI provider 默认 base URL 改为配置或环境变量；测试 fixture 与生产配置边界清晰 | 2026-05-19；前端登录 URL 改为 `VITE_NOTEAPP_LOGIN_CHECK_URL`；AI 预设改为 `VITE_NOTEAPP_AI_MODEL_OPTIONS_JSON` 或公开 fallback；桌面本地 AI 默认值支持 `NOTEAPP_LOCAL_SETTINGS_DEFAULT_AI_*`；验证前端 lint、settings fixture、桌面服务相关单测通过 |
| [x] HYG-05 | 已完成 | P2 | 删除未使用的演示视图 | `DashboardView.tsx`、旧 `WikiView.tsx` 等未接路由的 demo 页面清理完成；构建和 lint 通过 | 2026-05-19；删除未被引用的 `DashboardView.tsx` 和旧 `WikiView.tsx`；保留仍可从 Explorer 打开的 `ConflictsView.tsx`；`npm.cmd run lint` 通过 |
| [ ] HYG-06 | 未开始 | P1 | 收紧 `placeholder-v1` 加密兼容策略 | 生产路径缺少 `e2ee-v1` vault key 时明确失败；`placeholder-v1` 只允许测试或显式兼容模式使用 |  |
| [ ] HYG-07 | 未开始 | P2 | 明确 mobile 占位目录后续处理 | `clients/mobile` 要么进入正式移动端规划，要么从当前发布范围文档中明确排除 |  |
| [ ] HYG-08 | 未开始 | P2 | 清理剩余占位文档和 fixture 说明 | `docs/architecture`、测试计划、`packages/test-fixtures` 等占位 README 被补全、归档或删除 |  |

## 建议推进顺序

1. `HYG-01`：先从仓库里移除已跟踪 release 产物，降低后续提交噪音。
2. `HYG-02`：修复桌面 lockfile 的私有 registry，可提升新环境复现能力。
3. `HYG-03` 和 `HYG-04`：统一配置入口，减少硬编码环境差异。
4. `HYG-06`：收紧生产加密策略，避免误用 placeholder。
5. `POST-RC-01`：拿到签名证书后执行签名 release lane，作为公开发布最终门槛。
