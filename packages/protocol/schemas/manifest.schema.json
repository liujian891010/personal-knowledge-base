{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Manifest",
  "description": "Canonical synced manifest snapshot exported from filemap.json for one vault revision.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "vault_id",
    "revision",
    "base_revision",
    "created_by_device",
    "created_at",
    "files",
    "tombstones",
    "summary_hash"
  ],
  "properties": {
    "schema_version": {
      "type": "string",
      "const": "v1"
    },
    "vault_id": {
      "type": "string",
      "minLength": 1
    },
    "revision": {
      "type": "integer",
      "minimum": 0
    },
    "base_revision": {
      "type": "integer",
      "minimum": 0,
      "description": "Revision the client last fully applied before creating this manifest."
    },
    "created_by_device": {
      "type": "string",
      "minLength": 1
    },
    "created_at": {
      "type": "integer",
      "minimum": 0,
      "description": "UTC Unix timestamp in milliseconds."
    },
    "files": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/fileEntry"
      }
    },
    "tombstones": {
      "type": "array",
      "items": {
        "$ref": "./tombstone.schema.json"
      }
    },
    "summary_hash": {
      "type": "string",
      "minLength": 1,
      "description": "Stable digest of the canonical manifest."
    },
    "meta": {
      "type": "object",
      "description": "Reserved extension area. Must not affect canonical hashing unless explicitly versioned.",
      "additionalProperties": true
    }
  },
  "$defs": {
    "fileEntry": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "file_id",
        "path",
        "type",
        "content_hash",
        "blob_id",
        "size",
        "mtime"
      ],
      "properties": {
        "file_id": {
          "type": "string",
          "minLength": 1
        },
        "path": {
          "type": "string",
          "minLength": 1
        },
        "type": {
          "type": "string",
          "enum": [
            "note",
            "attachment",
            "ai_wiki",
            "ai_index",
            "ai_agents"
          ]
        },
        "content_hash": {
          "type": "string",
          "minLength": 1
        },
        "blob_id": {
          "type": "string",
          "minLength": 1
        },
        "size": {
          "type": "integer",
          "minimum": 0
        },
        "mtime": {
          "type": "integer",
          "minimum": 0,
          "description": "Source device local write time, stored as UTC Unix timestamp in milliseconds."
        },
        "mime_type": {
          "type": "string",
          "minLength": 1
        },
        "meta": {
          "type": "object",
          "additionalProperties": true
        }
      }
    }
  }
}
