# local-settings fixtures

Golden examples for `.noteapp/settings.json`.

`settings.json` is device-local by design. It stores UI and local runtime preferences only; sync connection settings still come from the desktop runtime environment.
